/** biome-ignore-all lint/style/useConsistentTypeDefinitions: this matches the input file format naming convention */
/** biome-ignore-all lint/style/useNamingConvention: this is to match the incoming file format */
export type Data = {
  Date: Date;
  Amount: number;
  Merchantname: string;
  Name: string;
};
